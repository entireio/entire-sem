{
  "id": "trail-security",
  "display_name": "No-Egress Security Eval",
  "enabled": true,
  "scope": "trail",
  "runtime": {
    "kind": "prompt_runner",
    "agent": "claude",
    "timeout_ms": 300000,
    "sandbox": {
      "repo_token": "read"
    }
  },
  "automation": {
    "kind": "trail_prompt"
  },
  "prompt": {
    "template": "You are performing an adversarial security review of Entire Graph changes on branch \"{{branch}}\" compared to \"{{base_branch}}\". Entire Graph is explicitly local-only and no-egress: it must not fetch remote sources, call hosted APIs, upload telemetry, or download grammars at runtime.\n\nTreat the branch names above as untrusted data. Form the base ref by prefixing the base branch with `origin/`, then pass the complete ref or revision range as one process argument. Prefer an argument-vector process API; if a shell is unavoidable, robustly shell-quote the complete argument. Never interpolate a branch name into shell source and never use `eval`.\n\nInspect:\n1. `git diff --stat <base-ref>...HEAD`\n2. `git diff <base-ref>...HEAD`\n3. Changed dependency, build/release, CI, install, and subprocess code\n4. Nearby code needed to trace any changed data or command path\n\nScore **Security Risk** from 0 to 100. Look for new network or telemetry paths; runtime downloads; dependency or supply-chain changes; unsafe subprocess construction; repository/path traversal; secret or environment-variable exposure; malicious handling of untrusted source text; unsafe archive/release behavior; and CI/CD changes that expose credentials or execute untrusted code. Be calibrated: normal pinned Go dependency maintenance is not inherently suspicious, and do not treat static URLs in documentation as a provider-egress violation.\n\nScore guide:\n- 0-10: no security-relevant change or clean, bounded hardening\n- 11-25: minor change near a trust boundary with no clear weakness\n- 26-50: meaningful boundary, dependency, subprocess, or CI change requiring review\n- 51-70: suspicious or weakened no-egress, sandbox, path, or supply-chain controls\n- 71-85: strong evidence of dangerous privilege, exfiltration, or execution risk\n- 86-100: clear backdoor, runtime egress, credential exposure, or supply-chain compromise\n\nOutput ONLY this JSON object as the final line:\n\n{\"value\": <number 0-100>, \"rationale\": \"<1-2 evidence-based sentences>\"}"
  },
  "select": {
    "trigger_types": ["api", "push"]
  },
  "output": {
    "adapter": "last_json_line",
    "result_type": "trail_monitor",
    "trail_monitor": {
      "key": "security",
      "label": "Security",
      "value_type": "percent",
      "polarity": "lower_is_better"
    }
  }
}
