Review the current branch changes and report actionable findings. Prioritize correctness, regressions, security, and missing test coverage. Do not make code changes.

Scope: review the commits unique to this branch vs origin/HEAD, plus any uncommitted changes in the working tree. Ignore code outside this scope.

Checkpoint context from commits in scope:
- 13a55a80ec0f: prompt: Try “entire review” again. If you don’t understand how it works, inspect the ../cli repo.
- cde5d8b348d8: prompt: Create a new branch to implement docs/semantic_provider_requirements.md. Write your progress into docs/progress-so-far.md. Make sure everything is tested, reviewed with "entire review", and commit/push along the way.
- 344d73e0a85b: prompt: Create a new branch to implement docs/semantic_provider_requirements.md. Write your progress into docs/progress-so-far.md. Make sure everything is tested, reviewed with "entire review", and commit/push along the way.

Use `entire explain <id>` for full checkpoint context, or `entire explain <id> --raw-transcript` for raw transcripts.

In-progress session context (uncommitted):
  019e7da4 Codex (touched: 2 files) prompt: Try “entire review” again. If you don’t understand how it works, inspect the ../cli repo.
  019e7f73 Codex (touched: 5 files) prompt: Task for Codex in `../entire-sem`: Fix Phase 1 provider contract compatibility with `entire-brain`. Context: - `entire-brain` invokes the semantic provider as: - `entire sem doctor --json` - `entire sem snapshot --repo <path> --format ndjson --no-network` - Local integration testing found two provider incompatibilit...
  019e7f73 Codex prompt: You are a helpful assistant. You will be presented with a user prompt, and your job is to provide a short title for a task that will be created from that prompt. The task usually has to do with coding work, such as fixing a bug, changing a feature, or answering a question about a codebase. Generate a concise UI titl...